/**
 * Hook-related type definitions
 * Centralizes types used by custom hooks across the application
 */

// Debug Mode hook types
export interface DebugMode {
  isEnabled: boolean;
  pageData: any;
}

// Add other hook-related types here as needed
